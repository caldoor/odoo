<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_partner_form" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.caldoor</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="groups_id" eval="[(4, ref('account.group_account_invoice'))]"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_view_partner_invoices']" position="after">
                <button type="object" class="oe_stat_button" icon="fa-dollar" name="action_view_open_invoices"
                    attrs="{'invisible': ['|', ('customer', '=', False), ('invoice_open_amount', '=', 0)]}" context="{'default_partner_id': active_id}">
                    <div class="o_form_field o_stat_info">
                        <span class="o_stat_value">
                            <field name="invoice_open_amount" widget='monetary' options="{'currency_field': 'currency_id'}"/>
                        </span>
                        <span class="o_stat_text">Open Invoices</span>
                    </div>
                </button>
            </xpath>
        </field>
    </record>
    
    <record id="action_view_open_invoices" model="ir.actions.act_window">
        <field name="name">Open Invoices</field>
        <field name="res_model">account.invoice</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form,kanban,calendar,graph,pivot</field>
        <field name="domain">[('state', '=', 'open'), ('type', 'in', ('out_invoice', 'out_refund'))]</field>
        <field name="search_view_id" ref="account.view_account_invoice_filter"/>
    </record>
</odoo>